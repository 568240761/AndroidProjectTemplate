<?xml version="1.0" encoding="utf-8"?>
<resources>
    <paths>
        <!--
            配置root-path，这样可以读取到sd卡和应用被系统分身后的目录；
            否则获取微信分身保存的图片，就会导致 java.lang.IllegalArgumentException。

            root-path：表示根目录，“/”。
        -->
        <root-path
            name="root"
            path="" />

        <!--
            表示 content.getFileDir() 获取到的目录
            /data/data/<包名>/files
        -->
        <!--        <files-path-->
        <!--            name="files-path"-->
        <!--            path="." />-->

        <!--
            表示 content.getCacheDir() 获取到的目录
            /data/data/<包名>/cache
        -->
        <!--        <cache-path-->
        <!--            name="cache-path"-->
        <!--            path="." />-->

        <!--
            表示Environment.getExternalStorageDirectory() 指向的目录
            /storage/emulate/0
        -->
        <!--        <external-path-->
        <!--            name="external_storage_root"-->
        <!--            path="." />-->

        <!--
            表示 ContextCompat.getExternalFilesDirs() 获取到的目录
            /storage/emulate/0/Android/data/<包名>/files
        -->
        <!--        <external-files-path-->
        <!--            name="external_file_path"-->
        <!--            path="." />-->

        <!--
            表示 ContextCompat.getExternalCacheDirs() 获取到的目录
            /storage/emulate/0/Android/data/<包名>/cache
        -->
        <!--                <external-cache-path-->
        <!--                    name="external_cache_path"-->
        <!--                    path="." />-->

        <!--
            表示 Context.getExternalMediaDirs() 获取到的目录
            /storage/emulate/0/Android/data/<包名>/media
            此目录仅在API 21+设备上可用
         -->
        <!--        <external-media-path-->
        <!--            name="external_media_path"-->
        <!--            path="." />-->
    </paths>
</resources>